<%= form_for(@book_cover_image) do |f| %>
  <% if @book_cover_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_cover_image.errors.count, "error") %> prohibited this book_cover_image from being saved:</h2>

      <ul>
      <% @book_cover_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bookurl %><br />
    <%= f.text_field :bookurl %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
